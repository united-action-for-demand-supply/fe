import React from 'react';
import { Container, Typography } from '@material-ui/core';

const Register = () => (
  <Container>
    <Typography>
      Register route
    </Typography>
  </Container>
);

export default Register;
