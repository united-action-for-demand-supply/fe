import React from 'react';
import { Container, Typography } from '@material-ui/core';

const Home = () => (
  <Container>
    <Typography>
      Home route
    </Typography>
  </Container>
);

export default Home;
