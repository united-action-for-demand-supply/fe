import React from 'react';
import { Container, Typography } from '@material-ui/core';

const Login = () => (
  <Container>
    <Typography>
      Login route
    </Typography>
  </Container>
);

export default Login;
